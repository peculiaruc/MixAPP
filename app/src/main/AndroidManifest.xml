<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.peculiaruc.mixapp">

    <application
        android:allowBackup="true"
        android:icon="@drawable/programing"
        android:label="@string/app_name"
        android:roundIcon="@drawable/programing"
        android:supportsRtl="true"
        android:theme="@style/Theme.MixAPP">
        <activity android:name=".ui.LogInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.DetailsActivity" />
        <activity android:name=".ui.ListViewActivity" />
        <activity android:name=".MainActivity">

        </activity>
    </application>

</manifest>